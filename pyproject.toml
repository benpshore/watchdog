[project]
name = "benpshore-watchdog"
version = "0.1.0"
description = "Cooperative health/heartbeat watchdog library for embedded health monitoring"
readme = "README.md"
authors = [
    { name = "Ben Shore", email = "147789778+benpshore@users.noreply.github.com" }
]
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=7.0", "ruff>=0.1.0"]

[build-system]
requires = ["uv_build>=0.12.17,<0.13.0"]
build-backend = "uv_build"
